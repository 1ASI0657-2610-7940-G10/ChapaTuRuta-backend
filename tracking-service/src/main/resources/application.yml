server:
  port: ${PORT:8082}

spring:
  application:
    name: tracking-service

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/tracking_db}
    username: ${DATABASE_USER:postgres}
    password: ${DATABASE_PASSWORD:khafna09}
    driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true

  data:
    redis:
      # Conexion directa a tu instancia de Upstash Redis en la nube
      host: ${REDIS_HOST:perfect-spider-122585.upstash.io}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:gQAAAAAAAd7ZAAIgcDJkMzdlZjI3ZTkwZDE0YTAzOGUwODJlMTA1OGM1MjljOQ}
      ssl:
        enabled: ${REDIS_SSL:true} # Obligatorio para Upstash

  rabbitmq:
    # Conexion directa a tu instancia de CloudAMQP en la nube
    host: ${RABBITMQ_HOST:jaragua.lmq.cloudamqp.com}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:jdqimjrx}
    password: ${RABBITMQ_PASSWORD:jsrL8bpV9TAzLUkwnSoiCj6MD6GfAEdK}
    virtual-host: ${RABBITMQ_VHOST:jdqimjrx}
    template:
      default-receive-queue: tracking.queue
      exchange: tracking.exchange
      routing-key: tracking.routing.key

springdoc:
  api-docs:
    path: /api-docs

  swagger-ui:
    path: /swagger-ui.html

mapbox:
  api-key: pk.eyJ1IjoiamhlY3RvcjYzMiIsImEiOiJjbW94amR0Y2QwMXh6MnFvaDhraGI1ZTMwIn0.V6nopXP1B4xIiI9wFwCuiw